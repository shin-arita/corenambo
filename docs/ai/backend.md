# バックエンド方針

> **このファイルは CLAUDE.md に統合済みのため縮小管理しています。**
>
> バックエンドに関するルール・アーキテクチャ方針・禁止事項・完了条件はすべて
> リポジトリルートの `CLAUDE.md` を参照してください。
>
> 既存リンクの破壊を避けるためにこのファイルは残していますが、
> 内容の更新はすべて `CLAUDE.md` 側で行います。

## 参照先

- ルール・禁止事項 → `CLAUDE.md` > Absolute Rules
- アーキテクチャ方針 → `CLAUDE.md` > Architecture Rules
- テスト完了条件 → `CLAUDE.md` > Quality Gate
- 設計判断の背景 → `docs/architecture/`
